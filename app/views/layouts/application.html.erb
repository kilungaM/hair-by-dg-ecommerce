<!DOCTYPE html>
<html>
  <head>
    <title>Hair By Dg Ecommerce</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
      :root {
        --gold: #D4AF37;
        --black: #000000;
        --white: #FFFFFF;
      }

      body {
        background-color: var(--white);
      }

      .navbar {
        background-color: var(--black) !important;
        border-bottom: 3px solid var(--gold);
      }

      .btn-primary {
        background-color: var(--gold);
        border-color: var(--gold);
        color: var(--black);
        font-weight: bold;
      }

      .btn-primary:hover {
        background-color: #B8960F;
        border-color: #B8960F;
        color: var(--black);
      }

      .btn-outline-primary {
        border-color: var(--gold);
        color: var(--gold);
      }

      .btn-outline-primary:hover {
        background-color: var(--gold);
        border-color: var(--gold);
        color: var(--black);
      }

      .text-primary {
        color: var(--gold) !important;
      }

      .card {
        border: 2px solid #e0e0e0;
        transition: all 0.3s;
      }

      .card:hover {
        border-color: var(--gold);
        box-shadow: 0 4px 12px rgba(212, 175, 55, 0.3);
        transform: translateY(-5px);
      }

      .badge.bg-danger {
        background-color: var(--gold) !important;
        color: var(--black);
      }

      .jumbotron {
        border: 3px solid var(--gold);
      }
    </style>
  </head>

  <body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container">
        <%= link_to "Hair by DG", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <%= link_to "Home", root_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Products", products_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Cart", cart_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Admin", admin_root_path, class: "nav-link" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="alert alert-info alert-dismissible fade show m-3" role="alert">
        <%= notice %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% end %>

    <% if flash[:success] %>
      <div class="alert alert-dismissible fade show m-3" role="alert" style="background-color: #D4AF37; color: #000;">
        <%= flash[:success] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% end %>

    <%= yield %>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>